# Synapse - Voice Commander for Test Automation

# AI & LLM
google-generativeai>=0.5.0

# Voice Recognition (optional - for voice mode)
SpeechRecognition>=3.10.0
# PyAudio - install separately: pipwin install pyaudio (Windows) or brew install portaudio && pip install pyaudio (macOS)

# gRPC
grpcio>=1.60.0
protobuf>=4.25.0

# Configuration
python-dotenv>=1.0.0

# Utilities
pydantic>=2.5.0
